/**
 * Test helpers. Not imported by the runtime entry points, so this code never
 * reaches a production bundle.
 */

export { checkStoreConformance } from "./conformance.ts";
export type { CheckResult, ConformanceHarness, ConformanceReport } from "./conformance.ts";
